# Public Modal API URL. After `modal deploy sft_modal.py` this is the URL printed
# next to the `api` function (it looks like https://<workspace>--lexforge-sft-qlora-api.modal.run).
NEXT_PUBLIC_MODAL_API=https://fadeiitj--lexforge-sft-qlora-api.modal.run
